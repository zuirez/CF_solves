#include <bits/stdc++.h>
using namespace std;

#define optimize() ios_base::sync_with_stdio(0);//cin.tie(0);cout.tie(0);
#define endl '\n'

int arr[10000];

int main()
{
    optimize();

    int n;
    cin>>n;

    for(int i=0; i<n; )
    {
        cout<<'a';
    }
}